#include <iostream>

using namespace std;

int mode(int*, int);
int freq(int);

int main()
{

    return 0;
}

int mode(int* arr, int len)
{
    int mode = -1;
    int* freq = new int[len];
    int cnt = 0;
    for(int i = 0; i < len; ++ i)
    {
        for(int k = 1; k < len ++ k)
        {
            if(*(arr + i) == *(arr + k) && )
        }

    }
    return mode;
}

