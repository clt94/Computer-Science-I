#include <iostream>

using namespace std;

int main()
{
    string input = "";
    getline(cin, input);
    strcpy(tab2, temp.c_str());
    cin.clear();
    input = "";
    getline(cin, input);
    char anagram2[input.length()] = input;
    cin.clear();
    char letters1[26];
    char letters2[26];
    for(unsigned int i = 0; i < anagram1.length(); ++ i)
    {
        tolower(letters1[i]);
        if(!ispunct(anagram1[i]) && !isspace(anagram[i])
        {
            letters1[i - 97];
        }
    }
    return 0;
}
